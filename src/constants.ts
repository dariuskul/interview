export enum Routes {
  Login = "/login",
  Users = "/items",
  Weak = "/items/weak",
  Reused = "/items/reused",
  Root = "/",
  Old = "/items/old",
}

export enum API {
  Login = "api/login",
  Items = "api/items",
  User = "api/user",
  Logout = "api/logout",
}

export enum Roles {
  read = "read",
  write = "write",
  amin = "amin",
}
